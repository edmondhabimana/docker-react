sudo: required

services:
  - docker

before_install:
  - docker build -t edmondhabimana/docker-react -f dockerfile.dev .

script:
  - docker run edmondhabimana/docker-react npm run test -- --coverage